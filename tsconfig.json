{
  "compilerOptions": {
    // ECMAScript 버전 설정
    "target": "ES2020",
    // 사용 가능한 라이브러리와 기능을 정의합니다.
    "lib": ["dom", "dom.iterable", "esnext"],
    // JavaScript 파일도 허용할지 여부를 설정합니다.
    "allowJs": true,
    // 라이브러리 검사를 생략합니다. (성능 향상)
    "skipLibCheck": true,
    // 엄격한 타입 검사 옵션을 비활성화합니다.
    "strict": false,
    // 파일 이름의 대소문자 일관성을 강제합니다.
    "forceConsistentCasingInFileNames": true,
    // 컴파일 결과를 출력하지 않도록 설정합니다.
    "noEmit": true,
    // 증분 컴파일을 활성화하여 빌드 성능을 향상시킵니다.
    "incremental": true,
    // 다른 모듈 시스템과의 상호 운용성을 위해 esModuleInterop 활성화합니다.
    "esModuleInterop": true,
    // 모듈 시스템을 ESNext로 설정합니다.
    "module": "esnext",
    // 모듈 해결 방법을 Node.js 기반으로 설정합니다.
    "moduleResolution": "node",
    // JSON 모듈을 해석할 수 있도록 설정합니다.
    "resolveJsonModule": true,
    // 각 파일을 독립된 모듈로 컴파일합니다.
    "isolatedModules": true,
    // JSX 문법을 사용할 때 변환 방식을 설정합니다.
    "jsx": "preserve",
    "baseUrl": "./src",
    "paths": {
      "@/*": ["./*"],
      "@pages/*": ["pages/*"],
      "@components/*": ["components/*"],
      "@utils/*": ["utils/*"],
      "@hooks/*": ["hooks/*"],
      "@types/*": ["types/*"]
    }
  },
  // 타입스크립트 파일 및 확장자가 .d.ts인 파일 포함
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx"],
  // 컴파일 제외할 디렉토리 설정
  "exclude": ["node_modules"]
}
